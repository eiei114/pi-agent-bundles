# Usage

Install from Git, preferably pinned to a tag:

```bash
pi install git:github.com/eiei114/pi-agent-bundles@v0.1.0
```

This repo can host multiple bundle folders. Use Pi package filters when an agent should load only one bundle.
