{
  "settings": {
    "toolPrefix": "server",
    "directTools": false,
    "idleTimeout": 10,
    "outputGuard": true
  },
  "mcpServers": {},
  "_comment": "ios-codex55-planner: planning/review; proxy-only MCP discovery by default. Add non-secret MCP server definitions here or use shared .mcp.json files. Do not commit tokens or OAuth state."
}
