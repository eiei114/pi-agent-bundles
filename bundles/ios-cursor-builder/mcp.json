{
  "settings": {
    "toolPrefix": "server",
    "directTools": false,
    "idleTimeout": 10,
    "outputGuard": true
  },
  "mcpServers": {
    "xcodebuildmcp": {
      "command": "npx",
      "args": [
        "-y",
        "xcodebuildmcp@2.6.2",
        "mcp"
      ],
      "lifecycle": "lazy",
      "idleTimeout": 10,
      "requestTimeoutMs": 300000,
      "directTools": false
    }
  },
  "_comment": "ios-cursor-builder: agent-local xcodebuildmcp server via pi-mcp-adapter. Keep secrets and auth state out of this file; use env vars or machine-local MCP override files for credentials."
}
